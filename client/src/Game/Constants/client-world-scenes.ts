// Presentation scenes are intentionally not gameplay worlds. Keep this
// registry for the baseline builder without promoting character select through
// collision, spatial, zone-transform, or server-world pipelines.
export const requiredClientWorldScenes = [] as const;
