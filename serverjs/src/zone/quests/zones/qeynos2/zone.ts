import type { ZoneQuestRegistry } from "../../../quest-registry.js";

export function registerZoneQuests(_quests: ZoneQuestRegistry): void {
  // Zone-global and named custom-event registrations live here.
}
