// The base smoke now covers login, character-list bootstrap, and zone negotiation
// using Shado sidecars/fixed packets. Keep this command as a compatibility entrypoint.
await import("./webtransport-smoke.js");
